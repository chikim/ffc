<h1>Club's rules</h1>

<% if flash[:notice] %>
  <h3><font color="red"><%= flash[:notice] %></font></h3>
<% end %>

<% if @rules.persisted? %>
  <div>
    <h3><%= @rules.title %></h3>
    <%= @rules.content.html_safe %>
  </div>
<% else %>
  <div>
    <h3><font color="red">Rules has not been created</font></h3>
  </div>
<% end %>

<div>
  <%= form_for @rules, url: update_rules_admin_introduction_path, method: :post do |f| %>
    <p>
    <%= f.label :title %>
    </br>
    <%= f.text_field :title %>
    </p>

    <p>
    <%= f.label :content %>
    </br>
    <%= f.text_area :content, class: :ckeditor %>
    </p>

    <p><%= f.submit %></p>
  <% end %>
</div>
